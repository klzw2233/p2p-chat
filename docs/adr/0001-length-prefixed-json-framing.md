# 0001 Length Prefixed JSON Framing

P2PCore 提供的是无消息边界的底层双向可靠字节流。本项目决定在应用层采用 4 字节大端整数长度前缀配合 UTF-8 JSON 编码的帧格式（`[len: u32][payload: json]`）来分发聊天消息。相比换行符定界（难以安全处理多行文本或控制字符）和二进制编解码（不够直观且难以人类调试），长度前缀 JSON 兼具天然防粘包、字段易扩展与调试友好的特性。
