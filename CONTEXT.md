# p2p-chat

基于 P2PCore 核心库构建的点对点（P2P）终端即时通信示例应用，演示端到端加密、会话协商、双向认证与安全码比对。

## Language

### 节点与身份

**Local Peer (本端节点)**:
运行当前客户端实例的端点，由本地保存或临时生成的 Identity Key 唯一标识。
_Avoid_: Client, 本地用户, 主机

**Remote Peer (远端对等端)**:
与本端建立或准备建立通信的目标节点，由其 Peer ID（32 字节公钥）标识。
_Avoid_: 好友, 联系人, 目标用户

**Peer ID (对等端标识)**:
公钥的 64 位十六进制（Hex）编码字符串，用于在 CLI 中发起连接或展示身份。
_Avoid_: 账号, UUID, 用户名

### 会话与命令

**Chat Session (聊天会话)**:
两个 Peer 之间建立的一条端到端加密、经过认证的双向活动通道。
_Avoid_: 房间, Channel, 聊天室

**Slash Command (斜杠命令)**:
用户在 REPL 终端中以 `/` 开头的指令，用于控制呼叫、信任验证及会话管理（如 `/dial`, `/sas`, `/verify`, `/quit`）。
_Avoid_: 控制指令, 系统调用

### 信任与验证

**SAS Display (安全码展示)**:
由双方公钥规范推导出的 8 组 5 位数字字符串，用户通过带外渠道口头或文字核对，确保无中间人攻击。
_Avoid_: 验证码, 一次性口令

**Verified Status (已验证状态)**:
用户通过 `/verify` 命令显式确认已核对 SAS 后的信任状态，之后连接将受严格公钥匹配保护。
_Avoid_: 认证成功, 好友认证
